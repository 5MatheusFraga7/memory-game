<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'memory_game', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'memory_game', 'data-turbolinks-track': 'reload' %>

    <title>Memory Game</title>
</head>
<body>
    <div class="container_cards">
        <div class="cards">
            <% @cards.each do |card|%>	  
                <div class="card" id = "card_<%= card["index"] %>" onclick='rotate_animation(this)'>	
                	<%= image_tag "op_icon.png", :class => "op_icon.png", :id => "op_icon.png" %> 
                </div>							       	
            <% end %>  

            <% @cards.shuffle.each do |card|%>	  
                <div class="card" id = "card2_<%= card["index"] %>" onclick='rotate_animation(this)'>	
                    <%= image_tag "op_icon.png", :class => "op_icon.png", :id => "op_icon.png" %> 
                </div>							       	
            <% end %>            
        </div>
    </div>
</body>
</html>